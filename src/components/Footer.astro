---
import Icon from './Icon.astro'
import { profile } from '../data/profile'
import { navLinks } from '../data/navigation'
import { SITE_NAME } from '../data/config'

const socialLinks = [
  profile.social.github && { href: profile.social.github, label: 'GitHub', icon: 'github' },
  profile.social.telegram && { href: profile.social.telegram, label: 'Telegram', icon: 'telegram' },
  { href: '/rss.xml', label: 'RSS', icon: 'rss' },
].filter(Boolean) as { href: string; label: string; icon: string }[]

const year = new Date().getFullYear()
---

<footer class="mt-32 border-t border-subtle">
  <div class="mx-auto max-w-[var(--container-grid)] px-5">

    {/* Main footer grid */}
    <div class="grid grid-cols-1 gap-12 py-16 md:grid-cols-12 md:gap-8">

      {/* Brand column */}
      <div class="md:col-span-5">
        <a href="/" class="inline-block font-[family-name:var(--font-heading)] text-2xl text-primary hover:text-accent transition-colors">
          {SITE_NAME}
        </a>
        <p class="mt-4 max-w-sm text-sm leading-relaxed text-secondary">
          {profile.bio}
        </p>
        <div class="mt-5 flex items-center gap-1.5 text-xs text-secondary">
          <Icon name="map-pin" size={14} />
          <span>{profile.location}</span>
        </div>
      </div>

      {/* Navigation column */}
      <div class="md:col-span-3 md:col-start-7">
        <h3 class="text-xs font-medium uppercase tracking-widest text-secondary mb-5">Navigate</h3>
        <ul class="space-y-3">
          {navLinks.map(({ href, label }) => (
            <li>
              <a
                href={href}
                class="text-sm text-primary/70 hover:text-accent transition-colors"
              >
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      {/* Connect column */}
      <div class="md:col-span-3">
        <h3 class="text-xs font-medium uppercase tracking-widest text-secondary mb-5">Connect</h3>
        <ul class="space-y-3">
          {socialLinks.map(({ href, label, icon }) => (
            <li>
              <a
                href={href}
                target={href.startsWith('/') ? undefined : '_blank'}
                rel={href.startsWith('/') ? undefined : 'noopener noreferrer'}
                class="inline-flex items-center gap-2 text-sm text-primary/70 hover:text-accent transition-colors"
              >
                <Icon name={icon} size={16} />
                <span>{label}</span>
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    {/* Divider */}
    <div class="border-t border-subtle"></div>

    {/* Bottom bar */}
    <div class="flex flex-col items-center justify-between gap-4 py-8 md:flex-row">
      <p class="text-xs text-secondary">
        &copy; {year} {profile.name}
      </p>
      <p class="text-xs text-secondary/50">
        Crafted with precision and dark roast coffee.
      </p>
    </div>

  </div>
</footer>
